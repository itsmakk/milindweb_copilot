{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "insforge": {
      "type": "local",
      "command": [
        "npx",
        "-y",
        "@insforge/mcp@latest"
      ],
      "environment": {
        "API_KEY": "${env:INSFORGE_API_KEY}",
        "API_BASE_URL": "${env:INSFORGE_BASE_URL:-https://fcpkj5u5.ap-southeast.insforge.app}"
      }
    }
  }
}
