{
  "name": "milindweb-api-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker — reverse proxy for mk9.in/api/* → Render",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --env production",
    "deploy:staging": "wrangler deploy --env staging",
    "login": "wrangler login",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.78.0",
    "typescript": "^5.6.0",
    "@cloudflare/workers-types": "^4.20241218.0"
  }
}
