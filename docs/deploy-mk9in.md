# Deployment — mk9.in (moved)

This guide used to live here. It has been split into two:

- **[`DEPLOY.md`](./DEPLOY.md)** — the three-click cloud deployment
  (Cloudflare Pages + Worker, Render Blueprint, InsForge bootstrap).
- **[`INSFORGE.md`](./INSFORGE.md)** — how the InsForge integration works
  (drivers, schema translation, auth, local dev, limits).

If you arrived looking for the "ship to mk9.in" runbook, go to
[`DEPLOY.md`](./DEPLOY.md).
